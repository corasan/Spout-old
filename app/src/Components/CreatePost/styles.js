import { StyleSheet } from 'react-native'
import { MAIN, TEXT } from '../lib/theme'

export default StyleSheet.create({
  closeModal: {
    height: 30,
    width: 30,
    position: 'absolute',
    right: 15,
    top: 15,
  },
})
